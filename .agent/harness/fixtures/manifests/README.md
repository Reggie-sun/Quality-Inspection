# Manifest Fixtures

This directory is reserved for sanitized schema fixtures. A live current-four manifest belongs only to its immutable run directory and must contain opaque input references rather than host paths or PDF bytes.
