# Fixtures

Only sanitized, deterministic fixture inputs belong here. Fixtures must contain no credentials, host source paths, real PDF bytes, or unsanitized Provider payloads. Fixture mode never authorizes external calls.
